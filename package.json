{
    "name": "mern-quicksave",
    "version": "1.0.0",
    "scripts": {
        "start": "node ./server/index.js",
        "heroku-prebuild": "npm --prefix ./client install && npm --prefix ./client build",
        "heroku-postbuild": "npm --prefix ./server install"
    }
}
