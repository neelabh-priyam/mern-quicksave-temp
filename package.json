{
    "name": "mern-quicksave",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "npm install --prefix ./server && npm install --prefix ./client && npm build --prefix ./client"
    }
}
